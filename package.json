{
  "name": "aws-practice-project",
  "version": "1.0.0",
  "description": "An AWS practice project.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
